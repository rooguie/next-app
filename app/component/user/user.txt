user CRUD 수행순서

1. 컴포넌트 분리 (폴더 생성) => user

2. userList.tsx 파일을 생성 => user 객체 정의 => 정의한 객체를 출력
    => userList 함수 생성 후 필요한 users 객체를 정의
    => 한 사람의 객체를 출력하는 User컴포넌트 생성
    => props로 user 객체를 전달하여 출력
    => props로 전달되는 객체 타입을 정의
    => user 객체타입, props 객체 타입 / 두 타입 모두 정의


    -----------------------------------------------------
    user CRUD
    -app / type / user.ts 파일 생성 => userType 정의 내보내기

    1. UserList2.tsx 파일 생성 => CRUD가 일어나는 대상
        => users 객체의 변화를 주기 위해 useState()로 users를 관리
        
        UserList2 => UserList1 => User 순서대로 컴포넌트 작성
        UserList2 => CreateUser

    2. UserList1 컴포넌트 생성 => users 배열을 props로 받아서 컴포넌트 생성
        => users props에 대한 타입을 정의
        => users.map으로 출력 => User 컴포넌트로 출력하는

    3. User 컴포넌트 생성 => user의 객체를 props로 받아서 컴포넌트 생성
        => user props에 대한 타입을 정의
        => 출력

    4. CreateUser 컴포넌트 생성 => input 2개  / btn 1개(추가 버튼)
        => userList2 CreateUser 사용할 inputs의 useState 생성
        => props로 username, email 전달
        => CreateUser props 받기 => props type 정의 => value 설정
        => onChange / onCreate => user 객체를 생성 => users 추가

    5. UserList2 삭제 기능 추가 => onRemove 함수 생성 후 => 함수 전달
        => UserList1 props로 전달 => USer props로 전달
        => x 버튼에 onClick으로 onRemove함수 실행

    6. UserList2 토글 기능 추가 => onToggle 함수 생성 후 => 함수 전달
        => UserList1 props로 전달 => User props로 전달
        => <b> 태그에 onclick으로 onToggle함수 실행